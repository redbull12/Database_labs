Server [localhost]:
Database [postgres]:
Port [5432]:
Username [postgres]:
Пароль пользователя postgres:
psql (17.6)
ПРЕДУПРЕЖДЕНИЕ: Кодовая страница консоли (866) отличается от основной
                страницы Windows (1251).
                8-битовые (русские) символы могут отображаться некорректно.
                Подробнее об этом смотрите документацию psql, раздел
                "Notes for Windows users".
Введите "help", чтобы получить справку.

postgres=# CREATE TABLE students (
postgres(# student_id INTEGER PRIMARY KEY,
postgres(# name VARCHAR(100),
postgres(# email VARCHAR(100) UNIQUE
postgres(# );
CREATE TABLE
postgres=# CREATE TABLE students2 (
postgres(# student_id INTEGER,
postgres(# name VARCHAR(100),
postgres(# email VARCHAR(100)
postgres(# );
CREATE TABLE
postgres=# ALTER TABLE students2 ADD CONSTRAINT pk_students PRIMARY KEY (student_id);
ALTER TABLE
postgres=# CREATE TABLE students3 (
postgres(# students_id SERIAL PRIMARY KEY,
postgres(# name VARCHAR(100) NOT NULL,
postgres(# email VARCHAR(100) UNIQUE,
postgres(# faculty VARCHAR(50)
postgres(# );
CREATE TABLE
postgres=# INSERT INTo students (name, email, faculty) VALUES
postgres-# ('Alice Johnson', 'alice@alatoo.edu.kg', 'Computer Science'),
postgres-# ('Bob Smith', 'bob@alatoo.edu.kg', 'Mathematics'),
postgres-#
postgres-# ('Carol Davis', 'carol@alatoo.edu.kg', 'Computer Science');
ОШИБКА:  столбец "faculty" в таблице "students" не существует
СТРОКА 1: INSERT INTo students (name, email, faculty) VALUES
                                        
postgres=# INSERT INTO students3 (name, email, faculty) VALUES
postgres-# ('Carol Davis', 'carol@alatoo.edu.kg', 'Computer Science');
INSERT 0 1
postgres=# CREATE TABLE course_enrollments (
postgres(# student_id INTEGER,
postgres(# course_id INTEGER,
postgres(# semester VARCHAR(50),
postgres(# enrollment_date DATE DEFAULT CURRENT_DATE,
postgres(# grade CHAR(2),
postgres(# PRIMARY KEY (student_id, course_id< semester)
postgres(# );
ОШИБКА:  ошибка синтаксиса (примерное положение: "<")
СТРОКА 7: PRIMARY KEY (student_id, course_id< semester)
                                            ^
postgres=# CREATE TABLE course_enrollments (
postgres(# student_id INTEGER,
postgres(# course_id INTEGER,
postgres(# semester VARCHAR(50),
postgres(# enrollment_date DATE DEFAULT CURRENT_DATE,
postgres(# grade CHAR(2),
postgres(# PRIMARY KEY (student_id, course_id, semester)
postgres(# );
CREATE TABLE
postgres=# INSERT INTO course_enrollments (student_id, course_id, semester, grade) VALUES
postgres-# (1, 101, '2024-Spring', 'A'),
postgres-# (1, 102, '2024-Spring', 'B+'),
postgres-# (2, 101, '2024-Spring', 'A-');
INSERT 0 3
postgres=# CREATE TABLE products (
postgres(# product_id SERIAL PRIMARY KEY,
postgres(# prosuct_name VARCHAR(150) NOT NULL,
postgres(# price DECIMAL (10,2)
postgres(# );
CREATE TABLE
postgres=# CREATE TABLE transactions (
postgres(# transaction_id BIGSERIAL PRIMARY KEY,
postgres(# amount DECIMAL(10,2),
postgres(# transaction_date TIMESTAMP DEFAULT CURRENT_TIMESTAMP
postgres(# );
CREATE TABLE

postgres=# CREATE TABLE orders (
postgres(# order_id INTEGER GENERATED ALWAYS AS IDENTITY PRIMARY KEY,
postgres(# customer_name VARCHAR(100),
postgres(# order_date DEFAULT DATE CURRENT_DATE
postgres(# );
ОШИБКА:  ошибка синтаксиса (примерное положение: "DEFAULT")
СТРОКА 4: order_date DEFAULT DATE CURRENT_DATE
                     ^
postgres=# CREATE TABLE orders (
postgres(# order_id INTEGER GENERATED ALWAYS AS IDENTITY PRIMARY KEY,
postgres(# customer_name VARCHAR(100),
postgres(# order_date DATE DEFAULT CURRENT_DATE
postgres(# );
CREATE TABLE
postgres=# CREATE TABLE customers (
postgres(# customer_id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
postgres(# customer_name VARCHAR(100),
postgres(# email VARCHAR(100)
postgres(# );
CREATE TABLE
postgres=# INSERT INTO customers (customer_id, customer_name, email) VALUES (1000, 'VIP customer', 'vip@example.com');
INSERT 0 1
postgres=#